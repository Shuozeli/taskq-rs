//! taskq-cli: taskq-rs operator CLI (migrate, namespace admin, quota, purge, replay).
//!
//! Phase 0 scaffold. Implementation lands in subsequent phases.

fn main() {
    println!("taskq-cli placeholder");
}
